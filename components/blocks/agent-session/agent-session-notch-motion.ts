/** Shared arrival timing for a collapsed Agent Session notch and its rail row. */
export const AGENT_SESSION_NOTCH_ARRIVAL = {
	duration: 0.25,
	ease: [0, 0.4, 0, 1] as [number, number, number, number],
};
